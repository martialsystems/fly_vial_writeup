# Agent notes: fly_vial_writeup

MIT. This git is a note, not a finding tree.

Do not restamp fly_vial `@e2e22b7` or F = 0.524 / 0.034.

The SHA index is gist `12835f747d6360781f3cc7f91f243178`. That page may gain citation columns (status, object, n / seeds, DOI). Locked SHA, question, and finding text stay frozen.

Do not fold vampire trees into this note. Do not pin GraphForge here. Do not add this git as a child row of fly_vial on the finding index.

Verify-before-done is the finish gate.

## Verify

`python3 ~/agent_laws_verify_before_done/vbd_gate.py check --app-root . --claim-done`

Do not use stock `/usr/bin/python3 -m pytest`.
